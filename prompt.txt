I found the X++ model and confirmed that Dynamics’ تاريخ تقديم الطلب is PAR_Assignment.ApplicationDate. I added a custom service that creates the request with the form’s defaults, deployed it to the local AOS model, and verified that submitAssignment appears in its WSDL. The localhost popup and API now use that service contract. TypeScript, .NET, and X++ compilation passed.

Localhost:3000 is not active on the new API yet. Automatic approval review blocked my command to stop and replace its running backend, and that process locks the Release DLL. Stop the current localhost server, run dotnet build backend/D365.Ess.Api.csproj -c Release -p:UseAppHost=false, then restart npm run dev. I validated the new API separately; I did not create a real assignment record during testing.